# Copy this file to `keystore.properties` and fill in your values to build a
# signed release locally. `keystore.properties` is git-ignored — never commit it.
# See RELEASING.md for how to generate the keystore.
storeFile=/absolute/path/to/inkride-release.jks
storePassword=
keyAlias=inkride
keyPassword=
